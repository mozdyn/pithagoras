export const BROWSER_READING_RULE = "Browser actions return status without an automatic page snapshot. browser_snapshot({}) returns the full accessibility snapshot with no imposed depth limit. Use it to understand an unfamiliar page. For follow-up reads, browser_find with text or regex locates relevant content and current element references; browser_snapshot({target: ref}) reads a specific section. An explicitly depth-limited snapshot may omit deeper content: remove the depth limit before assuming content is absent. Verify important action outcomes with a focused search or snapshot.";

export const BROWSER_SCREENSHOT_RULE = "For browser screenshots, omit filename so the tool returns actual image content; screenshots are still saved under an automatic path. A saved-file link alone is not an image you can see. Never infer visual details from that link; obtain inline image content before describing the screenshot.";
